# Happy-LLM 项目环境配置说明

## 环境信息

- **环境路径**: `D:\conda_envs\happy-llm`
- **Python 版本**: 3.10.19
- **创建日期**: 2025-11-05

## 激活环境

在 PowerShell 或命令行中使用以下命令激活环境：

```bash
conda activate D:\conda_envs\happy-llm
```

## 已安装的主要依赖包

### 深度学习框架
- torch (2.9.0)
- torchvision (0.24.0)
- transformers (4.57.1)
- datasets (4.4.1)
- tokenizers (0.22.1)
- accelerate (1.11.0)

### 训练相关
- trl (0.24.0)
- sentence-transformers (5.1.2)

### 数据处理
- pandas (2.3.3)
- numpy (2.2.6)
- scikit-learn (1.7.2)
- scipy (1.15.3)

### Web & API
- openai (2.7.1)
- streamlit (1.51.0)
- beautifulsoup4 (4.14.2)

### RAG 相关
- tiktoken (0.12.0)
- pypdf2 (3.0.1)

### 监控与日志
- swanlab (0.6.13)
- rich (13.9.4)
- psutil (7.1.3)

### 其他工具
- matplotlib (3.10.7)
- nltk (3.9.2)
- requests (2.32.5)
- pydantic (2.12.4)

## 关于 DeepSpeed

⚠️ **注意**: DeepSpeed 在 Windows 上安装可能会遇到兼容性问题，因此未包含在此环境中。

如果你需要使用 DeepSpeed（第六章训练流程需要），建议：

1. **使用 Linux 系统或 WSL2**:
   ```bash
   # 在 Linux/WSL2 环境中安装
   pip install deepspeed
   ```

2. **在 Windows 上**:
   - 可以跳过 DeepSpeed，使用其他分布式训练方案（如 accelerate）
   - 或者参考 DeepSpeed 官方文档的 Windows 安装指南

## 各章节依赖说明

### Chapter 2 - Transformer 基础
✅ 已满足所有依赖

### Chapter 5 - 动手搭建大模型
✅ 已满足所有依赖（包含 trl, swanlab）

### Chapter 6 - 训练流程实践
⚠️ 缺少 DeepSpeed（可选，见上述说明）

### Chapter 7 - 大模型应用
✅ 已满足所有依赖
- Agent: streamlit, openai 等已安装
- RAG: tiktoken, pypdf2 等已安装

## 验证安装

激活环境后，可以运行以下命令验证安装：

```python
python -c "import torch; print(f'PyTorch版本: {torch.__version__}')"
python -c "import transformers; print(f'Transformers版本: {transformers.__version__}')"
python -c "import streamlit; print(f'Streamlit版本: {streamlit.__version__}')"
```

## 如何使用不同章节的代码

激活环境后，直接进入对应章节的代码目录运行即可：

```bash
# 激活环境
conda activate D:\conda_envs\happy-llm

# 进入章节目录
cd docs/chapter5/code
python ddp_pretrain.py

# 或者运行 Agent 示例
cd docs/chapter7/Agent
streamlit run web_demo.py
```

## 卸载环境

如果需要删除环境，使用以下命令：

```bash
conda remove --prefix D:\conda_envs\happy-llm --all
```

## 额外依赖

如果在运行某些代码时遇到缺少依赖的问题，可以手动安装：

```bash
conda activate D:\conda_envs\happy-llm
pip install [包名]
```

## 支持的 CUDA

如果你有 NVIDIA GPU，当前安装的 PyTorch 2.9.0 支持 CUDA。可以通过以下命令检查 CUDA 是否可用：

```python
python -c "import torch; print(f'CUDA可用: {torch.cuda.is_available()}')"
```

---

**注意**: 本环境整合了项目中所有章节的依赖，适合完整学习整个课程。如果只需要特定章节的依赖，可以只安装对应的 `requirements.txt` 文件。

